workspace:
  base: /go
  path: src/github.com/horis233/kubelet-server-csr-approver

 pipeline:
  deps:
    image: golang:1.11
    commands:
      - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
      - ./test.sh

  docker:
    group: image
    registry: quay.io
    image: plugins/docker
    secrets: [ docker_username, docker_password ]
    repo: quay.io/horis233/kubelet-server-csr-approver-amd64
    dockerfile: Dockerfile
    auto_tag: true

  docker-arm:
    group: image
    registry: quay.io
    image: plugins/docker
    secrets: [ docker_username, docker_password ]
    repo: quay.io/horis233/kubelet-server-csr-approver-arm64
    dockerfile: Dockerfile
    build_args:
      - ARCH=arm64
    auto_tag: true